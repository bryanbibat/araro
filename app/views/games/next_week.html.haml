%h1 A new week begins...

%p 
  You've paid 
  = number_to_currency current_user.weekly_worker_pay
  to your workers. You also spent
  = number_to_currency current_user.weekly_overhead
  for other expenses.

= render(partial: "user", object: current_user)
